{
    "name": "My Codespace",
    "build": {
      "dockerfile": "Dockerfile",
      "args": {
        "VARIABLE_NAME": "SECRET_VALUE"
      }
    },
    "env": {
      "MY_SECRET": "${REPLICATE_API_KEY}"
    }
  }
  